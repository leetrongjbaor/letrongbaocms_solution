@model CMS.Data.Entities.Banner
@{
    ViewData["Title"] = "Sửa Banner";
}

<div class="container mt-4">
    <h2 class="text-primary fw-bold mb-3">Sửa Banner</h2>
    <form asp-action="Edit" method="post" enctype="multipart/form-data" class="card card-body shadow-sm">
        @Html.AntiForgeryToken()
        <input asp-for="Id" type="hidden" />
        <input asp-for="CreatedDate" type="hidden" />
        <partial name="_BannerForm" model="Model" />
        <div class="mt-3">
            <button type="submit" class="btn btn-primary">Cập nhật</button>
            <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
        </div>
    </form>
</div>
